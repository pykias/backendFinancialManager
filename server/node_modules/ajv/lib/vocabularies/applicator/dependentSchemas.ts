import type {CodeKeywordDefinition} from "ajv/lib/types"
import {validateSchemaDeps} from "ajv/lib/vocabularies/applicator/dependencies"

const def: CodeKeywordDefinition = {
  keyword: "dependentSchemas",
  type: "object",
  schemaType: "object",
  code: (cxt) => validateSchemaDeps(cxt),
}

export default def
